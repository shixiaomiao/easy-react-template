import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default className Demo extends Component {
    static PropTypes = {

    };

    constructor(props) {
        super(props);
        this.state = {

        };
    }

    render() {
        return (<div>{'内容'}</div);
    }
}
